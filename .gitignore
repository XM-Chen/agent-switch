# Generated platform adapter files (Trellis-managed; .trellis/ itself is tracked,
# with runtime state ignored by .trellis/.gitignore)
.agents/
.claude/
.codex/
.cursor/
.opencode/
.pi/
.reasonix/
.trae/
.zcode/
AGENTS.md

# Build artifacts
dist/
node_modules/
src-tauri/target/

# Tauri updater signing keys (private key must never be committed;
# public key belongs in tauri.conf.json, not as a repo file copy)
*.key
*.key.pub
