# spec 更新、全量门禁与归档

## Goal

更新 spec/backend（provider 数据模型约定、双模式接管语义、is_current 互斥规则）与 spec/frontend（切换器页面约定）；全量门禁 cargo fmt/clippy -D warnings/test + npm build/test；记录 session journal；归档 6 子任务+父任务。

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
